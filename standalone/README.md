# publish standalone boundary

This shell owns the module's direct input flow. Authentication, persistence, Core services, and production adapters are injected by the private host during assembly. Cross-module links are optional and are provided by the main workbench.
