# FrontMind publish

Public source projection for the publish module.

The module is independently type-checkable and buildable with pnpm. The private host supplies CoreServices and the runtime adapter; this repository never contains provider credentials, customer data, or private deployment configuration.
