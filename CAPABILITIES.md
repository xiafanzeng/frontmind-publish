# 能力与验收范围

module/ 包含业务页面、API、持久化、任务处理和所属工作流。standalone/ 提供独立入口、合成预览和开发域名装配；vendor/ 由主仓维护。

## 本地

pnpm install --frozen-lockfile、pnpm typecheck、pnpm test、pnpm build 已在本次源码上验证。pnpm dev 使用标识明确的合成样例，不连接线上数据库或付费供应商。

## 开发域名

私有运行入口提供固定测试工作区。真实保存、文件及任务功能在开发域名单独验收；发布状态以 /api/version 返回的 moduleSha/coreSha 为准。

## 需要运行配置的能力

供应商执行需要对应测试凭据。监控任务执行、媒体外发及域名发布不能凭本地预览判定通过；普通 ZIP 发布不触发这些付费或外发操作。品牌官网的旧草稿设置、预览分析和部件配置保留原能力边界，不作为新增真实建站能力。
